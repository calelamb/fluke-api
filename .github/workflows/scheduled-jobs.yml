name: Production scheduled jobs

on:
  workflow_dispatch:
    inputs:
      job:
        description: Production job to run
        required: true
        default: all
        type: choice
        options:
          - all
          - acartia
          - gbif
          - predictions
  schedule:
    - cron: '15 */6 * * *'
    - cron: '15 2 * * 0'
    - cron: '0 4 * * *'

permissions:
  contents: read

env:
  NODE_ENV: production
  DATABASE_URL: ${{ secrets.PRODUCTION_DATABASE_URL }}
  DIRECT_URL: ${{ secrets.PRODUCTION_DIRECT_URL }}
  JWT_SECRET: ${{ secrets.SCHEDULED_JOB_JWT_SECRET }}
  WEB_ORIGIN: https://fluke-pnw.vercel.app
  API_PUBLIC_ORIGIN: https://fluke-api.onrender.com
  PUBLIC_READ_ORIGIN: https://fluke-pnw.vercel.app
  IDENTIFIER_SERVICE_URL: https://identifier.fluke.invalid
  ENABLE_ACCOUNTS: "false"
  IDENTIFIER_MODE: "disabled"
  ENABLE_SUBMISSIONS: "false"
  STORAGE_BACKEND: s3
  OBJECT_STORAGE_ACCESS_KEY_ID: ${{ secrets.OBJECT_STORAGE_ACCESS_KEY_ID }}
  OBJECT_STORAGE_BUCKET: ${{ secrets.OBJECT_STORAGE_BUCKET }}
  OBJECT_STORAGE_ENDPOINT: ${{ secrets.OBJECT_STORAGE_ENDPOINT }}
  OBJECT_STORAGE_FORCE_PATH_STYLE: ${{ secrets.OBJECT_STORAGE_FORCE_PATH_STYLE }}
  OBJECT_STORAGE_REGION: ${{ secrets.OBJECT_STORAGE_REGION }}
  OBJECT_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.OBJECT_STORAGE_SECRET_ACCESS_KEY }}

jobs:
  acartia:
    if: >-
      ${{ github.event.schedule == '15 */6 * * *' ||
          (github.event_name == 'workflow_dispatch' &&
           (inputs.job == 'all' || inputs.job == 'acartia')) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    concurrency:
      group: fluke-production-acartia
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:generate
      - run: pnpm jobs:acartia
      - run: pnpm verify:production-data
      - run: pnpm warm:public acartia

  gbif:
    if: >-
      ${{ github.event.schedule == '15 2 * * 0' ||
          (github.event_name == 'workflow_dispatch' &&
           (inputs.job == 'all' || inputs.job == 'gbif')) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    concurrency:
      group: fluke-production-gbif
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:generate
      - run: pnpm jobs:gbif
      - run: pnpm verify:production-data
      - run: pnpm warm:public gbif

  predictions:
    if: >-
      ${{ github.event.schedule == '0 4 * * *' ||
          (github.event_name == 'workflow_dispatch' &&
           (inputs.job == 'all' || inputs.job == 'predictions')) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    concurrency:
      group: fluke-production-predictions
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:generate
      - run: pnpm jobs:predictions
      - run: pnpm verify:production-data
      - run: pnpm warm:public predictions
